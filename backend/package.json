{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Backend for the Writing Authenticity prototype: receives and stores writing-session metadata.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@google/genai": "^2.18.0",
    "better-sqlite3": "^12.4.1",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
